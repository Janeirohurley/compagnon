/**
 * GitHub Agent tools index (static stub for Mastra build).
 *
 * MCP tools load at runtime from the declarative registry
 * (mcp.servers.json) via src/mastra/mcp/registry.ts. This file
 * satisfies Mastra's fs-agent build import.
 */
const githubToolsIndex: Record<string, unknown> = {};

export default githubToolsIndex;
