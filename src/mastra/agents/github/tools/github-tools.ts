/**
 * GitHub Agent tools (static stub for Mastra build).
 *
 * The GitHub Agent loads MCP GitHub tools at runtime via
 * src/mastra/mcp/github-tools.ts. This file only exists to
 * satisfy Mastra's fs-agent build process which imports it.
 */
const githubTools: Record<string, unknown> = {};

export default githubTools;
