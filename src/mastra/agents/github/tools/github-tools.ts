/**
 * GitHub Agent tools (static stub for Mastra build).
 *
 * The GitHub Agent loads MCP GitHub tools at runtime from the
 * declarative registry (mcp.servers.json) via src/mastra/mcp/registry.ts.
 * This file only exists to satisfy Mastra's fs-agent build process.
 */
const githubTools: Record<string, unknown> = {};

export default githubTools;
