/**
 * Outline Agent tools (stub for Mastra fs-agent build).
 *
 * MCP tools load at runtime from the declarative registry
 * (mcp.servers.json) via src/mastra/mcp/registry.ts.
 */
const outlineTools: Record<string, unknown> = {};

export default outlineTools;
